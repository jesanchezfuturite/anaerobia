---
export const prerender = false

import Layout from '../../layouts/Layout.astro'
import { getGeneral } from '../../lib/general.js'
import fallback from '../../data/soluciones/sistemas-de-pretratamiento.json'

const general = await getGeneral()

// Contenido administrable: se consulta la API del Admin (Laravel) en cada
// visita (SSR). Si la API no responde, se usa el respaldo local (fallback).
const API_URL = import.meta.env.ADMIN_API_URL ?? 'http://127.0.0.1:8000'
let d = fallback
try {
    const res = await fetch(`${API_URL}/api/v1/soluciones/sistemas-de-pretratamiento`, { signal: AbortSignal.timeout(5000) })
    if (res.ok) d = (await res.json()).data
} catch {
    // API no disponible: la página se sirve con el contenido de respaldo
}

// Logos de certificaciones: contenido compartido del sitio que llega en el
// mismo payload de la API (se editan una sola vez en la página de Inicio).
const certificados = d.certificados ?? fallback.certificados ?? []

// SEO Metadata
const seoTitle = d.seo?.title
const seoDescription = d.seo?.description

// Iconos (SVG paths) de los items de Desafíos (diseño fijo; cada item puede tener más de un path)
const desafioIcons = [
    ['M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z'],
    ['M13.19 8.688a4.5 4.5 0 011.242 7.244l-4.5 4.5a4.5 4.5 0 01-6.364-6.364l1.757-1.757m13.35-.622l1.757-1.757a4.5 4.5 0 00-6.364-6.364l-4.5 4.5a4.5 4.5 0 001.242 7.244', 'M19.5 19.5l-15-15'],
    ['M12 6v12m-3-2.818l.879.659c1.171.879 3.07.879 4.242 0 1.172-.879 1.172-2.303 0-3.182C13.536 12.219 12.768 12 12 12c-.725 0-1.45-.22-2.003-.659-1.106-.879-1.106-2.303 0-3.182.553-.44 1.278-.659 2.003-.659.725 0 1.45.22 2.003.659l.415.33M21 12a9 9 0 11-18 0 9 9 0 0118 0z'],
]
// Iconos (SVG paths) de las tarjetas de Diferenciadores (diseño fijo)
const diferenciadorIcons = [
    'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z',
    'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z',
    'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z',
    'M9.75 3.104v5.714a2.25 2.25 0 0 1-.659 1.591L5 14.5M9.75 3.104c-.251.023-.501.05-.75.082m.75-.082a24.301 24.301 0 0 1 4.5 0m0 0v5.714c0 .597.237 1.17.659 1.591L19.8 15.3M14.25 3.104c.251.023.501.05.75.082M19.8 15.3l-1.57.393A9.065 9.065 0 0 1 12 15a9.065 9.065 0 0 0-6.23-.693L5 14.5m14.8.8 1.402 1.402c1.232 1.232.65 3.318-1.067 3.611A48.309 48.309 0 0 1 12 21c-2.773 0-5.491-.235-8.135-.687-1.718-.293-2.3-2.379-1.067-3.61L5 14.5',
]
// Iconos (SVG paths) de los Sectores industriales (diseño fijo)
const sectorIcons = [
    'M9 17a2 2 0 11-4 0 2 2 0 014 0zM19 17a2 2 0 11-4 0 2 2 0 014 0z M13 16V6a1 1 0 00-1-1H4a1 1 0 00-1 1v10a1 1 0 001 1h1m8-1a1 1 0 01-1 1H9m4-1V8a1 1 0 011-1h2.586a1 1 0 01.707.293l3.414 3.414a1 1 0 01.293.707V16a1 1 0 01-1 1h-1m-6-1h1',
    'M12 19l9 2-9-18-9 18 9-2zm0 0v-8',
    'M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z M15 12a3 3 0 11-6 0 3 3 0 016 0z',
    'M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4',
    'M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6',
    'M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4',
]
// Iconos (SVG paths) de los Beneficios operativos (diseño fijo)
const beneficioIcons = [
    'M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1',
    'M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z',
    'M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15',
    'M5 13l4 4L19 7',
    'M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z',
    'M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z',
]
// Ids de las pestañas de Especificaciones Técnicas (diseño fijo, usados por el script de tabs)
const tabIds = ['tab-desengrasado', 'tab-decapado', 'tab-fosfatado', 'tab-pasivacion', 'tab-enjuague-secado']
// Numeración de las pestañas técnicas visibles (las inactivas se ocultan)
let _tabN = 0
const tabNums = (d.tipos?.tabs ?? []).map((t) => (t.active !== false ? String(++_tabN).padStart(2, '0') : null))
// Un icono puede ser código SVG (se incrusta y hereda el color de la marca) o
// una imagen subida (se muestra tal cual, solo ajustada al tamaño).
const esIconoSvg = (icono) => typeof icono === 'string' && icono.trimStart().startsWith('<svg')

---

<Layout title={seoTitle} description={seoDescription}>
    <main class="bg-industrial-gray min-h-screen text-brand-navy">
        <!-- 01. HEADER / HERO -->
        <section class="relative min-h-[60vh] flex items-center pt-36 pb-20 bg-brand-navy overflow-hidden">
            <!-- Background Image with Overlay -->
            <div class="absolute inset-0 z-0">
                <img src={d.hero.image} alt="Equipo de Pretratamiento" class="w-full h-full object-cover opacity-60 scale-105" />
                <div class="absolute inset-0 bg-gradient-to-t from-brand-navy via-brand-navy/85 to-brand-navy/60"></div>
            </div>

            <div class="container mx-auto px-6 relative z-10">
                <div class="max-w-4xl hero-section-text">
                    <!-- Breadcrumbs -->
                    <nav class="flex items-center gap-2 text-xs md:text-sm text-gray-400 font-semibold uppercase tracking-widest mb-6">
                        <a href="/" class="text-gray-300 hover:text-brand-lime transition-colors">Inicio</a>
                        <span>/</span>
                        <span class="text-gray-400">Soluciones</span>
                        <span>/</span>
                        <span class="text-brand-lime">Pretratamiento</span>
                    </nav>

                    <span class="inline-block bg-brand-lime/10 text-brand-lime border border-brand-lime/20 rounded px-3 py-1 text-xs font-semibold uppercase tracking-widest mb-6">{d.hero.badge}</span>
                    <h1 class="text-white font-saira italic font-semibold uppercase text-4xl sm:text-5xl md:text-6xl lg:text-7xl leading-[1.1] mb-6">{d.hero.title}</h1>
                    <p class="text-gray-300 text-lg md:text-xl font-light max-w-3xl leading-relaxed whitespace-pre-wrap">
                        {d.hero.description}
                    </p>
                </div>
            </div>
        </section>

        <!-- 02. INTRODUCCIÓN AL PRETRATAMIENTO -->
        <section class="py-16 bg-white px-6 md:px-12 relative overflow-hidden" id="introduccion">
            <div class="container mx-auto">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
                    <div class="space-y-8 intro-text">
                        <div>
                            <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full"> Ingeniería y tecnología </span>
                            <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4 mb-6 leading-tight">{d.intro.title}</h2>
                            {
                                d.intro.paragraphs.map((p) => (
                                    <p class="text-gray-600 leading-relaxed font-light text-base md:text-lg whitespace-pre-wrap">{p.text}</p>
                                ))
                            }
                        </div>

                    </div>
                    <div class="relative intro-image group">
                        <div class="relative rounded-lg overflow-hidden shadow-2xl">
                            <img src={d.intro.image} alt="Pretratamiento de Superficies" class="w-full h-auto object-cover scale-125 transform hover:scale-[1.3] transition-transform duration-700" />
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- 03. IMPORTANCIA EN EL CICLO -->
        <section class="py-16 bg-industrial-gray px-6 md:px-12 border-t border-gray-200/50" id="importancia">
            <div class="container mx-auto">
                <div class="text-center max-w-3xl mx-auto mb-16 pain-points-header">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.desafios.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.desafios.title}</h2>
                    <p class="text-gray-600 mt-4 font-light whitespace-pre-wrap">{d.desafios.description}</p>
                </div>

                <div class="flex flex-col lg:flex-row gap-8 lg:h-[700px] w-full max-w-7xl mx-auto pain-points-grid items-stretch">
                    <!-- Column 1: Image (45%) -->
                    <div class="w-full lg:w-[45%] relative rounded-3xl overflow-hidden shadow-2xl h-[400px] lg:h-auto shrink-0 group">
                        <img src={d.desafios.image} alt="Pretratamiento" class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
                        <div class="absolute inset-0 bg-brand-navy/10 mix-blend-multiply"></div>
                    </div>

                    <!-- Column 2: Cards (20%) -->
                    <div class="w-full lg:w-[20%] flex flex-col justify-between gap-4 shrink-0 h-full">
                        {
                            d.desafios.items.map((t) => (
                                <div class="relative bg-white rounded-2xl shadow-[0_8px_30px_rgb(0,0,0,0.12)] flex flex-col justify-center p-4 lg:p-5 flex-1 min-h-[100px]">
                                    <p class="text-base lg:text-lg font-saira font-bold italic text-brand-blue leading-snug text-center whitespace-pre-wrap">{t.text}</p>
                                </div>
                            ))
                        }
                    </div>

                    <!-- Column 3: Content (35%) -->
                    <div class="w-full lg:w-[35%] flex flex-col justify-between space-y-4 shrink-0 lg:pl-2 h-full py-2">
                        {
                            d.desafios.items.map((item, i) => (
                                <div class="space-y-2 flex-1 flex flex-col justify-center">
                                    <div class="flex items-center gap-3">
                                        <div class="text-brand-lime shrink-0">
                                            {item.icon && !esIconoSvg(item.icon) ? (
                                                <img src={item.icon} alt={item.title} class="w-6 h-6 object-contain" />
                                            ) : item.icon ? (
                                                <Fragment set:html={item.icon} />
                                            ) : (
                                                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                {(desafioIcons[i] ?? desafioIcons[0]).map((p) => (
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={p} />
                                                ))}
                                            </svg>
                                            )}
                                        </div>
                                        <h3 class="font-saira font-bold italic text-brand-navy text-lg md:text-xl uppercase">{item.title}</h3>
                                    </div>
                                    <p class="text-sm text-gray-600 leading-relaxed font-light whitespace-pre-wrap">{item.description}</p>
                                </div>
                            ))
                        }
                    </div>
                </div>
            </div>
        </section>

        <!-- 03.5. PRIMER CTA PERSONALIZADO -->
        <section class="py-16 bg-brand-navy relative overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-r from-brand-navy via-brand-navy to-brand-blue/50 opacity-90"></div>
            <div class="container mx-auto px-6 relative z-10 flex flex-col md:flex-row items-center justify-between gap-8">
                <div class="text-left">
                    <h2 class="text-white text-2xl md:text-3xl font-saira italic font-semibold uppercase">{d.cta1.title}</h2>
                    <p class="text-gray-300 text-sm md:text-base font-light mt-2 max-w-xl whitespace-pre-wrap">{d.cta1.description}</p>
                </div>
                <a href={d.cta1.button_url} class="bg-brand-lime text-brand-navy font-bold uppercase tracking-widest px-8 py-4 rounded hover:brightness-110 transition-all shadow-lg shrink-0 text-center">{d.cta1.button_label}</a>
            </div>
        </section>

        <!-- 04. CONFIGURACIONES DE INGENIERÍA -->
        <section class="py-16 bg-gradient-to-b from-gray-100 to-white px-6 md:px-12 relative overflow-hidden" id="ingenieria">
            <div class="container mx-auto">
                <div class="max-w-5xl mb-10 section-solucion-header">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.ingenieria.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.ingenieria.title}</h2>
                    <p class="mt-6 text-gray-600 font-light text-lg whitespace-pre-wrap">
                        {d.ingenieria.description}
                    </p>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mt-10 text-left items-stretch">
                    <!-- Bento Card 1 (Large Left) -->
                    <div class="bg-brand-navy rounded-3xl p-10 lg:p-14 flex flex-col justify-between relative overflow-hidden group hover:-translate-y-1 transition-transform duration-300 shadow-2xl min-h-[500px] h-full">
                        <!-- Image background -->
                        <div class="absolute inset-0 bg-cover bg-center bg-no-repeat transition-transform duration-700 group-hover:scale-105" style={`background-image: url('${d.ingenieria.cards[0].image}')`}></div>
                        <!-- Overlay azul -->
                        <div class="absolute inset-0 bg-[#003141]/85 group-hover:bg-[#003141]/75 transition-colors duration-500"></div>

                        <!-- Decorative background element -->
                        <div class="absolute -right-20 -top-20 w-64 h-64 bg-brand-lime opacity-20 blur-3xl rounded-full group-hover:scale-150 transition-transform duration-700"></div>

                        <div class="absolute top-6 right-6 lg:top-8 lg:right-8 w-20 h-20 bg-brand-lime/20 text-brand-lime rounded-2xl flex items-center justify-center z-20">
                            {d.ingenieria.cards[0].icon && !esIconoSvg(d.ingenieria.cards[0].icon) ? (
                                    <img src={d.ingenieria.cards[0].icon} alt={d.ingenieria.cards[0].title} class="w-10 h-10 object-contain" />
                                ) : d.ingenieria.cards[0].icon ? (
                                    <Fragment set:html={d.ingenieria.cards[0].icon} />
                                ) : (
                                    <svg class="w-10 h-10" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"
                                ></path>
                                <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            </svg>
                                )}
                        </div>
                        <div class="relative z-10 mt-auto">
                            <div>
                                <h3 class="font-saira font-semibold italic text-white text-3xl md:text-4xl uppercase mb-4 leading-tight">{d.ingenieria.cards[0].title}</h3>
                                <p class="text-lg text-gray-200 font-light leading-relaxed whitespace-pre-wrap">
                                    {d.ingenieria.cards[0].description}
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- Right Column for Bento Cards 2 & 3 -->
                    <div class="flex flex-col gap-6 h-full">
                        <!-- Bento Card 2 -->
                        <div class="bg-brand-navy rounded-3xl p-8 lg:p-10 flex flex-col justify-center relative overflow-hidden group hover:-translate-y-1 transition-transform duration-300 h-full shadow-xl min-h-[250px]">
                            <!-- Image background -->
                            <div class="absolute inset-0 bg-cover bg-center bg-no-repeat transition-transform duration-700 group-hover:scale-105" style={`background-image: url('${d.ingenieria.cards[1].image}')`}></div>
                            <!-- Overlay azul -->
                            <div class="absolute inset-0 bg-[#003141]/85 group-hover:bg-[#003141]/75 transition-colors duration-500"></div>

                            <div class="absolute top-6 right-6 lg:top-8 lg:right-8 w-14 h-14 bg-brand-lime/20 shadow-sm text-brand-lime rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300 z-20">
                                {d.ingenieria.cards[1].icon && !esIconoSvg(d.ingenieria.cards[1].icon) ? (
                                    <img src={d.ingenieria.cards[1].icon} alt={d.ingenieria.cards[1].title} class="w-10 h-10 object-contain" />
                                ) : d.ingenieria.cards[1].icon ? (
                                    <Fragment set:html={d.ingenieria.cards[1].icon} />
                                ) : (
                                    <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        d="M9.813 15.904 9 18.75l-.813-2.846a4.5 4.5 0 0 0-3.09-3.09L2.25 12l2.846-.813a4.5 4.5 0 0 0 3.09-3.09L9 5.25l.813 2.846a4.5 4.5 0 0 0 3.09 3.09L15.75 12l-2.846.813a4.5 4.5 0 0 0-3.09 3.09ZM18.259 8.715 18 9.75l-.259-1.035a3.375 3.375 0 0 0-2.455-2.456L14.25 6l1.036-.259a3.375 3.375 0 0 0 2.455-2.456L18 2.25l.259 1.035a3.375 3.375 0 0 0 2.456 2.456L21.75 6l-1.035.259a3.375 3.375 0 0 0-2.456 2.456ZM16.894 20.567 16.5 21.75l-.394-1.183a2.25 2.25 0 0 0-1.423-1.423L13.5 18.75l1.183-.394a2.25 2.25 0 0 0 1.423-1.423l.394-1.183.394 1.183a2.25 2.25 0 0 0 1.423 1.423l1.183.394-1.183.394a2.25 2.25 0 0 0-1.423 1.423Z"
                                    ></path>
                                </svg>
                                )}
                            </div>
                            <div class="relative z-10">
                                <h3 class="font-saira font-semibold italic text-white text-2xl uppercase leading-tight pr-16 mb-6">{d.ingenieria.cards[1].title}</h3>
                                <p class="text-base text-gray-200 font-light leading-relaxed whitespace-pre-wrap">{d.ingenieria.cards[1].description}</p>
                            </div>
                        </div>

                        <!-- Bento Card 3 -->
                        <div class="bg-brand-navy rounded-3xl p-8 lg:p-10 shadow-xl border border-transparent flex flex-col justify-center relative overflow-hidden group hover:-translate-y-1 transition-transform duration-300 h-full min-h-[250px]">
                            <!-- Image background -->
                            <div class="absolute inset-0 bg-cover bg-center bg-no-repeat transition-transform duration-700 group-hover:scale-105" style={`background-image: url('${d.ingenieria.cards[2].image}')`}></div>
                            <!-- Overlay azul -->
                            <div class="absolute inset-0 bg-[#003141]/85 group-hover:bg-[#003141]/75 transition-colors duration-500"></div>

                            <div class="absolute top-6 right-6 lg:top-8 lg:right-8 w-14 h-14 bg-brand-lime/20 shadow-sm text-brand-lime rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300 z-20">
                                {d.ingenieria.cards[2].icon && !esIconoSvg(d.ingenieria.cards[2].icon) ? (
                                    <img src={d.ingenieria.cards[2].icon} alt={d.ingenieria.cards[2].title} class="w-10 h-10 object-contain" />
                                ) : d.ingenieria.cards[2].icon ? (
                                    <Fragment set:html={d.ingenieria.cards[2].icon} />
                                ) : (
                                    <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M6.429 9.75 2.25 12l4.179 2.25m0-4.5 5.571 3 5.571-3m-11.142 0L2.25 7.5 12 2.25l9.75 5.25-4.179 2.25m0 0L12 17.25 6.429 12.75m11.142 0-4.179 2.25m0 0L12 21.75 6.429 17.25"></path>
                                </svg>
                                )}
                            </div>
                            <div class="relative z-10">
                                <h3 class="font-saira font-semibold italic text-white text-2xl uppercase leading-tight pr-16 mb-6">{d.ingenieria.cards[2].title}</h3>
                                <p class="text-base text-gray-200 font-light leading-relaxed whitespace-pre-wrap">
                                    {d.ingenieria.cards[2].description}
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card 4 (Pasivación personalizada) -->
                <div class="bg-brand-navy rounded-3xl p-8 lg:p-10 relative overflow-hidden group border border-brand-navy/50 mt-6 shadow-xl w-full flex flex-col md:flex-row items-center justify-between gap-8">
                    <!-- Image background -->
                    <div class="absolute inset-0 bg-cover bg-center bg-no-repeat transition-transform duration-700 group-hover:scale-105" style={`background-image: url('${d.ingenieria.cards[3].image}')`}></div>
                    <!-- Overlay azul -->
                    <div class="absolute inset-0 bg-[#003141]/85 group-hover:bg-[#003141]/80 transition-colors duration-500"></div>

                    <div class="absolute right-0 top-0 w-64 h-64 bg-brand-lime/10 rounded-full blur-3xl -mr-20 -mt-20 z-10"></div>
                    <div class="absolute left-6 top-6 lg:left-10 lg:top-10 w-16 h-16 bg-brand-lime/20 shadow-sm text-brand-lime rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300 z-20">
                        {d.ingenieria.cards[3].icon && !esIconoSvg(d.ingenieria.cards[3].icon) ? (
                                    <img src={d.ingenieria.cards[3].icon} alt={d.ingenieria.cards[3].title} class="w-10 h-10 object-contain" />
                                ) : d.ingenieria.cards[3].icon ? (
                                    <Fragment set:html={d.ingenieria.cards[3].icon} />
                                ) : (
                                    <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75m-3-7.036A11.959 11.959 0 0 1 3.598 6 11.99 11.99 0 0 0 3 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285Z"
                            ></path>
                        </svg>
                                )}
                    </div>

                    <div class="relative z-10 w-full pl-0 pt-20 lg:pt-0 lg:pl-24">
                        <h3 class="font-saira font-semibold italic text-white text-2xl lg:text-3xl uppercase leading-tight mb-4">{d.ingenieria.cards[3].title}</h3>
                        <p class="text-base lg:text-lg text-gray-200 font-light leading-relaxed whitespace-pre-wrap">{d.ingenieria.cards[3].description}</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- 08. VENTAJAS COMPETITIVAS -->
        <section class="py-16 bg-white px-6 md:px-12 border-t border-gray-100 relative overflow-hidden" id="diferenciadores">
            <div class="container mx-auto">
                <div class="text-center max-w-3xl mx-auto mb-16 diferenciadores-header">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.extras.diferenciadores.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.extras.diferenciadores.title}</h2>
                    <p class="text-gray-600 font-light mt-4 whitespace-pre-wrap">{d.extras.diferenciadores.description}</p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-8 diferenciadores-grid max-w-4xl mx-auto">
                    {
                        d.extras.diferenciadores.cards.map((dif, i) => (
                            <div class="bg-white p-8 rounded-2xl shadow-[0_8px_30px_rgb(0,0,0,0.12)] transition-shadow duration-300 hover:shadow-[0_8px_30px_rgb(0,0,0,0.2)] group flex flex-col justify-between">
                                <div class="space-y-4">
                                    <div class="flex items-center gap-4">
                                        <div class="text-brand-lime flex items-center justify-center shrink-0">
                                            {dif.icon && !esIconoSvg(dif.icon) ? (
                                                <img src={dif.icon} alt={dif.title} class="w-6 h-6 object-contain" />
                                            ) : dif.icon ? (
                                                <Fragment set:html={dif.icon} />
                                            ) : (
                                                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={diferenciadorIcons[i] ?? diferenciadorIcons[0]} /> </svg>
                                            )}
                                        </div>
                                        <h3 class="font-saira font-semibold italic text-brand-navy text-xl uppercase group-hover:text-brand-blue transition-colors leading-tight">{dif.title}</h3>
                                    </div>
                                    <p class="text-sm text-gray-500 leading-relaxed font-light whitespace-pre-wrap">{dif.description}</p>
                                </div>
                            </div>
                        ))
                    }
                </div>
            </div>
        </section>

        <!-- 05. ESPECIFICACIONES TÉCNICAS -->
        <section class="py-16 bg-brand-lime/5 px-6 md:px-12 border-y border-gray-200/50 relative overflow-hidden" id="fases">
            <img src="/images/soluciones/fondo-seccion.png" alt="" class="absolute top-1/2 -translate-y-1/2 -right-32 lg:-right-20 w-[400px] lg:w-[600px] opacity-10 invert mix-blend-multiply object-contain pointer-events-none z-0" />

            <div class="container mx-auto relative z-10">
                <div class="mb-16 text-center md:text-left">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.tipos.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4 mb-8">{d.tipos.title}</h2>
                </div>

                <!-- Tabs Navigation -->
                <div class="flex w-full border-b border-gray-200 mb-16 overflow-x-auto snap-x hide-scrollbar" id="tech-tabs-nav">
                    {
                        d.tipos.tabs.map((tab, i) => tab.active !== false && (
                            <button
                                class={`tech-tab-btn ${tabNums[i] === '01' ? 'active ' : ''}snap-start flex-1 py-4 px-2 border-b-2 ${tabNums[i] === '01' ? 'border-brand-lime bg-brand-lime/10 rounded-t-xl text-brand-lime' : 'border-transparent bg-transparent rounded-t-xl text-gray-400 hover:text-brand-lime hover:bg-brand-lime/5'} flex flex-col items-center justify-center gap-1 cursor-pointer select-none font-saira font-semibold italic text-sm md:text-base uppercase tracking-wider transition-colors min-w-[140px]`}
                                data-tab={tabIds[i]}
                            >
                                <span class="text-2xl md:text-3xl font-light not-italic font-sans">{tabNums[i]}</span>
                                <span>{tab.label}</span>
                            </button>
                        ))
                    }
                </div>

                <!-- Tabs Content Panels -->
                <div id="tech-tabs-content" class="relative w-full">
                    {
                        d.tipos.tabs.map((tab, i) => tab.active !== false && (
                            <div id={tabIds[i]} class={`tech-tab-panel transition-all duration-500 ${tabNums[i] === '01' ? 'opacity-100 transform translate-y-0 relative z-10' : 'opacity-0 transform translate-y-4 absolute top-0 left-0 w-full z-0 pointer-events-none'}`}>
                                <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-start">
                                    <div class="w-full relative rounded-3xl overflow-hidden shadow-2xl h-[300px] md:h-[400px]">
                                        <img src={tab.image} alt={tab.title} class="w-full h-full object-cover absolute inset-0" />
                                    </div>
                                    <div class="flex flex-col pb-8">
                                        <h3 class="font-saira font-semibold italic text-brand-navy text-3xl md:text-4xl uppercase mb-4">{tab.title}</h3>
                                        <p class="text-base text-gray-600 font-light leading-relaxed mb-6 whitespace-pre-wrap">{tab.description}</p>
                                        {i === 1 && <p class="text-base text-gray-600 font-light leading-relaxed mb-6">Pueden utilizar ácidos como:</p>}
                                        <ul class="space-y-4 text-gray-600 font-light text-sm">
                                            {tab.items.map((item) => (
                                                <li class="flex items-start gap-3">
                                                    <svg class="w-6 h-6 text-brand-lime shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                                        <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                                                    </svg>
                                                    <span class="whitespace-pre-wrap">{item.title ? <><strong class="whitespace-pre-wrap">{item.title}</strong> {item.description}</> : item.description}</span>
                                                </li>
                                            ))}
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        ))
                    }
                </div>
            </div>
        </section>
        <!-- 07. SECTORES INDUSTRIALES Y GUÍA TÉCNICA -->
        <section class="py-16 bg-white px-6 md:px-12 border-t border-gray-200/50" id="sectores">
            <div class="container mx-auto max-w-7xl">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">
                    <!-- Columna Izquierda: Aplicaciones B2B -->
                    <div class="flex flex-col">
                        <div class="mb-8 text-center lg:text-left">
                            <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.extras.sectores.badge}</span>
                            <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.extras.sectores.title}</h2>
                            <p class="text-gray-600 mt-4 font-light text-lg whitespace-pre-wrap">{d.extras.sectores.description}</p>
                        </div>
                        <div class="grid grid-cols-2 gap-y-6 gap-x-4">
                            {
                                d.extras.sectores.items.map((sector, i) => (
                                    <div class="flex items-center gap-3 text-left">
                                        <div class="text-brand-lime shrink-0">
                                            {sector.icon && !esIconoSvg(sector.icon) ? (
                                                <img src={sector.icon} alt={sector.text} class="w-6 h-6 object-contain" />
                                            ) : sector.icon ? (
                                                <Fragment set:html={sector.icon} />
                                            ) : (
                                                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={sectorIcons[i] ?? sectorIcons[0]} />
                                                </svg>
                                            )}
                                        </div>
                                        <span class="font-saira font-bold text-brand-navy uppercase text-sm leading-tight whitespace-pre-wrap">{sector.text}</span>
                                    </div>
                                ))
                            }
                        </div>
                    </div>

                    <!-- Columna Derecha: Guía Técnica -->
                    <div class="w-full h-full flex">
                        <div class="bg-brand-navy rounded-3xl p-8 md:p-10 text-white shadow-2xl relative overflow-hidden w-full flex flex-col justify-center">
                            <div class="absolute top-0 right-0 w-64 h-64 bg-brand-lime/10 rounded-full blur-3xl -mr-20 -mt-20"></div>

                            <h2 class="text-2xl md:text-3xl font-saira italic font-semibold text-white uppercase mb-4 relative z-10">{d.extras.guia_tecnica.title}</h2>
                            <p class="text-gray-300 font-light text-base mb-8 relative z-10 whitespace-pre-wrap">{d.extras.guia_tecnica.description}</p>

                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 relative z-10">
                                {
                                    d.extras.guia_tecnica.items.map((variable) => (
                                        <div class="flex items-center gap-3">
                                            <svg class="w-5 h-5 text-brand-lime shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                            </svg>
                                            <span class="font-semibold text-sm uppercase tracking-wide whitespace-pre-wrap">{variable.text}</span>
                                        </div>
                                    ))
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- 09. BENEFICIOS OPERATIVOS -->
        <section class="py-16 bg-white px-6 md:px-12 border-t border-gray-200/50" id="beneficios">
            <div class="container mx-auto max-w-6xl">
                <div class="text-center max-w-3xl mx-auto mb-16 advantages-header">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.resultados.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.resultados.title}</h2>
                    <p class="text-gray-600 mt-4 font-light text-lg whitespace-pre-wrap">{d.resultados.description}</p>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-12 text-center advantages-grid">
                    {
                        d.resultados.cards.map((bene, i) => (
                            <div class="space-y-4 p-6 hover:-translate-y-1 transition-transform duration-300">
                                <div class="w-16 h-16 [clip-path:polygon(50%_0%,100%_25%,100%_75%,50%_100%,0%_75%,0%_25%)] bg-brand-lime/10 text-brand-lime flex items-center justify-center mx-auto">
                                    {bene.icon && !esIconoSvg(bene.icon) ? (
                                        <img src={bene.icon} alt="" class="w-8 h-8 object-contain" />
                                    ) : bene.icon ? (
                                        <Fragment set:html={bene.icon} />
                                    ) : (
                                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={beneficioIcons[i] ?? beneficioIcons[0]} /> </svg>
                                    )}
                                </div>
                                <h3 class="font-saira font-bold text-brand-navy text-lg italic uppercase whitespace-pre-wrap">{bene.text}</h3>
                            </div>
                        ))
                    }
                </div>
            </div>
        </section>

        <!-- 10. SEGUNDO CTA -->
        <section class="py-16 relative flex items-center justify-center overflow-hidden px-6">
            <img src={d.cta2.image} alt="Ingenieros interactuando con maquinaria" class="absolute top-1/2 left-0 w-full h-[130%] -translate-y-1/2 object-cover z-0" />
            <div class="absolute inset-0 bg-brand-navy/90 z-10 mix-blend-multiply"></div>

            <div class="container mx-auto max-w-4xl text-center space-y-6 relative z-20">
                <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-white uppercase tracking-wider">{d.cta2.title}</h2>
                <p class="text-gray-300 font-light text-base md:text-lg max-w-2xl mx-auto whitespace-pre-wrap">{d.cta2.description}</p>
                <div class="pt-8">
                    <a href={d.cta2.button_url} class="inline-block bg-brand-lime text-brand-navy font-bold uppercase tracking-widest px-10 py-4 rounded hover:bg-white transition-all duration-300 shadow-lg hover:-translate-y-1">{d.cta2.button_label}</a>
                </div>
            </div>
        </section>

        <!-- 11. NORMATIVIDAD -->
        <section class="py-16 bg-brand-navy text-white px-6 md:px-12" id="normas">
            <div class="container mx-auto max-w-6xl">
                <div class="flex flex-col lg:flex-row items-center justify-between gap-12">
                    <div class="lg:w-1/2 space-y-6 norms-text">
                        <div>
                            <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{general.normas?.badge}</span>
                            <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-white uppercase mt-4 leading-tight">{general.normas?.title}</h2>
                            <p class="text-gray-300 font-light leading-relaxed mt-4 whitespace-pre-wrap">
                                {general.normas?.description}
                            </p>
                        </div>

                        <p class="text-gray-400 font-light text-xs border-t border-white/10 pt-4 whitespace-pre-wrap">{general.normas?.text}</p>
                    </div>

                    <div class="lg:w-1/2 w-full">
                        <div class="grid grid-cols-2 sm:grid-cols-3 gap-4 norms-grid">
                            {
                                certificados.map((cert) => (
                                    <div class="bg-white rounded-xl p-4 flex flex-col items-center justify-center shadow-lg hover:shadow-xl hover:-translate-y-1 transition-all duration-300 group border border-transparent hover:border-brand-lime">
                                        <div class="h-14 w-full flex items-center justify-center mb-2">
                                            <img src={cert.logo} alt={`Certificación ${cert.name}`} class="max-h-full max-w-full object-contain transition-all duration-300" />
                                        </div>
                                    </div>
                                ))
                            }
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- 12. ALIANZAS / PARTNERS -->
        <section class="bg-white py-4 border-y border-gray-200 overflow-hidden flex items-center">
            <div class="container mx-auto px-6 flex flex-col md:flex-row items-center gap-6">
                <span class="text-gray-500 font-medium whitespace-nowrap text-sm mt-1 uppercase tracking-wide"> {general.aliados?.title} </span>
                <div class="swiper marquee-swiper w-full overflow-hidden relative">
                    <div class="swiper-wrapper flex items-center gap-16">
                        {[...(general.aliados?.logos ?? []), ...(general.aliados?.logos ?? [])].map((aliado) => (
                            <div class="swiper-slide !w-auto px-8 flex items-center justify-center">
                                <img src={aliado.image} alt={aliado.name} class="h-auto max-h-9 w-auto max-w-full object-contain opacity-60 grayscale hover:grayscale-0 transition-all" />
                            </div>
                        ))}
                    </div>
                </div>
            </div>
        </section>

        <!-- 13. FAQ (PREGUNTAS FRECUENTES) -->
        <section class="py-16 bg-industrial-gray px-6 md:px-12 border-b border-gray-200/50" id="faq">
            <div class="container mx-auto max-w-4xl">
                <div class="text-center mb-16 faq-header">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.faqs.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.faqs.title}</h2>
                </div>

                <div class="space-y-4">
                    {
                        d.faqs.items.map((faq, i) => (
                            <details name="faq" class="group border border-gray-300/80 rounded-xl bg-white overflow-hidden transition-all duration-300 open:shadow-md" open={i === 0}>
                                <summary class="flex justify-between items-center px-6 py-5 font-saira font-semibold italic text-brand-navy uppercase text-base md:text-lg cursor-pointer hover:bg-gray-50 transition-colors list-none select-none">
                                    <span>{faq.question}</span>
                                    <span class="w-6 h-6 rounded-full bg-brand-navy/5 flex items-center justify-center text-brand-navy font-bold text-xl after:content-['+'] group-open:after:content-['-']" />
                                </summary>
                                <div class="px-6 pb-6 pt-2 border-t border-gray-100 text-gray-600 font-light text-base leading-relaxed details-content-reveal">
                                    {faq.answer.includes('\n\n') ? faq.answer.split('\n\n').map((p, j, arr) => <p class={j < arr.length - 1 ? 'mb-4' : undefined}>{p}</p>) : faq.answer}
                                </div>
                            </details>
                        ))
                    }
                </div>
            </div>
        </section>

        <!-- 14. GALERÍA DE PROYECTOS -->
        <section class="py-16 bg-white px-6 md:px-12" id="galeria">
            <div class="container mx-auto">
                <div class="text-center max-w-3xl mx-auto mb-16 gallery-header">
                    <span class="text-brand-lime font-bold tracking-widest text-xs mb-2 uppercase inline-block bg-brand-lime/10 px-4 py-1.5 rounded-full">{d.galeria.badge}</span>
                    <h2 class="text-3xl md:text-4xl font-saira italic font-semibold text-brand-navy uppercase mt-4">{d.galeria.title}</h2>
                    <p class="text-gray-600 mt-4 font-light">Ejemplos de túneles de pretratamiento, tinas por inmersión y sistemas químicos llave en mano implementados en plantas del país.</p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 gallery-grid">
                    {
                        d.galeria.images.map((img) => (
                            <div class="relative overflow-hidden rounded-lg shadow group border border-gray-200">
                                <img src={img.image} alt={img.alt} class="w-full h-64 object-cover transform group-hover:scale-105 transition-transform duration-500" />
                            </div>
                        ))
                    }
                </div>
            </div>
        </section>
    </main>

    <!-- GSAP Animations for new elements -->
    <script is:inline>
        document.addEventListener('DOMContentLoaded', () => {
            if (typeof gsap !== 'undefined' && typeof ScrollTrigger !== 'undefined') {
                gsap.from('.hero-section-text > *', {
                    y: 35,
                    opacity: 0,
                    duration: 1,
                    stagger: 0.15,
                    ease: 'power3.out',
                })
                gsap.from('.intro-text > *', {
                    scrollTrigger: { trigger: '#introduccion', start: 'top 80%' },
                    y: 30,
                    opacity: 0,
                    duration: 0.8,
                    stagger: 0.12,
                    ease: 'power2.out',
                })
                gsap.from('.intro-image', {
                    scrollTrigger: { trigger: '#introduccion', start: 'top 80%' },
                    x: 40,
                    opacity: 0,
                    duration: 0.9,
                    ease: 'power2.out',
                })
                gsap.from('.pain-points-grid > div', {
                    scrollTrigger: { trigger: '#importancia', start: 'top 85%' },
                    y: 40,
                    opacity: 0,
                    duration: 0.8,
                    stagger: 0.15,
                    ease: 'power2.out',
                })
                gsap.from('#ingenieria .grid > div', {
                    scrollTrigger: { trigger: '#ingenieria', start: 'top 80%' },
                    y: 30,
                    opacity: 0,
                    duration: 0.7,
                    stagger: 0.15,
                    ease: 'power2.out',
                    clearProps: 'all',
                })
                gsap.from('#fases .grid > div', {
                    scrollTrigger: { trigger: '#fases', start: 'top 85%' },
                    y: 20,
                    opacity: 0,
                    duration: 0.5,
                    stagger: 0.1,
                    ease: 'back.out(1.7)',
                })

                gsap.from('#sectores .grid > div', {
                    scrollTrigger: { trigger: '#sectores', start: 'top 85%' },
                    scale: 0.9,
                    opacity: 0,
                    duration: 0.5,
                    stagger: 0.05,
                    ease: 'power2.out',
                })
                gsap.from('#diferenciadores .grid > div', {
                    scrollTrigger: { trigger: '#diferenciadores', start: 'top 85%' },
                    y: 30,
                    opacity: 0,
                    duration: 0.6,
                    stagger: 0.1,
                    ease: 'power2.out',
                })
                gsap.from('#beneficios .grid > div', {
                    scrollTrigger: { trigger: '#beneficios', start: 'top 85%' },
                    y: 20,
                    opacity: 0,
                    duration: 0.6,
                    stagger: 0.1,
                    ease: 'power2.out',
                })

                // Nuevas animaciones añadidas
                gsap.from('.norms-text > *', {
                    scrollTrigger: { trigger: '#normas', start: 'top 80%' },
                    y: 30,
                    opacity: 0,
                    duration: 0.8,
                    stagger: 0.12,
                    ease: 'power2.out',
                })
                gsap.from('.norms-grid > div', {
                    scrollTrigger: { trigger: '.norms-grid', start: 'top 85%' },
                    y: 20,
                    opacity: 0,
                    duration: 0.6,
                    stagger: 0.1,
                    ease: 'power2.out',
                    clearProps: 'opacity,transform',
                })
                gsap.from('#faq details', {
                    scrollTrigger: { trigger: '#faq', start: 'top 80%' },
                    y: 20,
                    opacity: 0,
                    duration: 0.6,
                    stagger: 0.1,
                    ease: 'power2.out',
                    clearProps: 'opacity,transform',
                })
                gsap.from('.gallery-grid > div', {
                    scrollTrigger: { trigger: '.gallery-grid', start: 'top 85%' },
                    y: 30,
                    opacity: 0,
                    duration: 0.7,
                    stagger: 0.12,
                    ease: 'power2.out',
                    clearProps: 'opacity,transform',
                })
            }

            // Tech Tabs Logic
            const tabBtns = document.querySelectorAll('.tech-tab-btn')
            const tabPanels = document.querySelectorAll('.tech-tab-panel')

            if (tabBtns.length > 0) {
                tabBtns.forEach((btn) => {
                    btn.addEventListener('click', () => {
                        const targetTab = btn.getAttribute('data-tab')

                        // Update buttons
                        tabBtns.forEach((b) => {
                            b.classList.remove('active', 'border-brand-lime', 'bg-brand-lime/10', 'text-brand-lime')
                            b.classList.add('border-transparent', 'bg-transparent', 'text-gray-400')
                        })
                        btn.classList.remove('border-transparent', 'bg-transparent', 'text-gray-400')
                        btn.classList.add('active', 'border-brand-lime', 'bg-brand-lime/10', 'text-brand-lime')

                        // Update panels
                        tabPanels.forEach((panel) => {
                            panel.classList.remove('opacity-100', 'translate-y-0', 'relative', 'z-10')
                            panel.classList.add('opacity-0', 'translate-y-4', 'absolute', 'top-0', 'left-0', 'w-full', 'z-0', 'pointer-events-none')
                        })

                        const targetPanel = document.getElementById(targetTab)
                        if (targetPanel) {
                            targetPanel.classList.remove('opacity-0', 'translate-y-4', 'absolute', 'top-0', 'left-0', 'w-full', 'z-0', 'pointer-events-none')
                            targetPanel.classList.add('opacity-100', 'translate-y-0', 'relative', 'z-10')
                        }
                    })
                })
            }

            // Inicializar Marquee Swiper
            if (typeof Swiper !== 'undefined') {
                new Swiper('.marquee-swiper', {
                    loop: true,
                    speed: 3000,
                    autoplay: { delay: 0, disableOnInteraction: false },
                    slidesPerView: 'auto',
                    spaceBetween: 10,
                    allowTouchMove: false,
                    freeMode: true,
                })
            }
        })
    
    // Si la primera pestaña técnica está inactiva, activar la primera visible
    document.addEventListener('DOMContentLoaded', () => {
        const techBtns = document.querySelectorAll('.tech-tab-btn')
        if (techBtns.length && !document.querySelector('.tech-tab-btn.active')) {
            techBtns[0].click()
        }
    })
</script>
</Layout>
